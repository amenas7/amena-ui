import { Meta } from '@storybook/blocks';

<Meta title="Componentes/Forms/Radio/Radio Example" />

# 📚 Code Example - sa-radio

Se puede usar los corchetes en las propiedades <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[status]`</span>, <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[required]`</span>, <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[disabled]`</span> para controlar el estado del botón desde una variable de tipo boolean.

### HTML Template
```html
<form [formGroup]="formRadioIndividual">
  <div class="row g-3">
    <div class="col-md-8">
      <div class="d-flex gap-4 align-items-start">
        <sa-radio
          name="plan"
          value="basic"
          formControlName="planSeleccionado"
          label="Plan Básico - $9.99/mes"
          helperText="Ideal para uso personal. Incluye 10GB de almacenamiento"
          size="md"
          [status]="radioStatus"
          [disabled]="isDisabled"
          [errorText]="
            formRadioIndividual.get('planSeleccionado')?.hasError('required') && formRadioIndividual.get('planSeleccionado')?.touched
              ? 'Debe seleccionar un plan'
              : ''
          "
          (valueChange)="onValueChange($event)"
          (change)="onChange($event)"
          (focusin)="onFocus($event)"
          (focusout)="onBlur($event)">
        </sa-radio>

        <sa-radio
          name="plan"
          value="pro"
          formControlName="planSeleccionado"
          label="Plan Pro - $29.99/mes"
          helperText="Para profesionales. Incluye 100GB y soporte prioritario"
          size="md"
          [status]="radioStatus"
          [disabled]="isDisabled"
          [errorText]="
            formRadioIndividual.get('planSeleccionado')?.hasError('required') && formRadioIndividual.get('planSeleccionado')?.touched
              ? 'Debe seleccionar un plan'
              : ''
          "
          (valueChange)="onValueChange($event)"
          (change)="onChange($event)"
          (focusin)="onFocus($event)"
          (focusout)="onBlur($event)">
        </sa-radio>
      </div>
    </div>

    <div class="col-md-4">
      <div class="mt-2">
        <p><strong>Estado del campo:</strong></p>
        <ul class="small">
          <li>Valor: {{ formRadioIndividual.get('planSeleccionado')?.value || 'vacío' }}</li>
          <li>Valid: {{ formRadioIndividual.get('planSeleccionado')?.valid }}</li>
          <li>Touched: {{ formRadioIndividual.get('planSeleccionado')?.touched }}</li>
          <li>Status:
            <sa-tag
              [text]="radioStatus"
              [type]="
                radioStatus === 'error'
                  ? 'danger'
                  : radioStatus === 'success'
                  ? 'success'
                  : 'secondary'
              "
              size="small">
            </sa-tag>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <div class="row g-3 mt-2">
    <div class="col-md-12">
      <div class="alert alert-info">
        <strong>Plan seleccionado:</strong> {{ planSeleccionadoTexto }}
      </div>
    </div>
  </div>

  <div class="row g-3 mt-2">
    <div class="col-md-12">
      <button class="btn btn-sm btn-outline-info me-2" (click)="isDisabled = !isDisabled">
        Toggle Disabled ({{ isDisabled }})
      </button>
    </div>
  </div>
</form>
```

### ⚡ TypeScript Component

```typescript
import { Component, OnInit } from '@angular/core';
import { FormBuilder, FormGroup, Validators } from '@angular/forms';

@Component({
  ......
})

export class ........ {

  // Formulario para el ejemplo con radios individuales
  formRadioIndividual!: FormGroup;

  // Variables para controles
  isDisabled: boolean = false;

  constructor(private fb: FormBuilder) {}

  ngOnInit(): void {
    // Inicializar formulario para radios individuales (sin required por defecto)
    this.formRadioIndividual = this.fb.group({
      planSeleccionado: ['']
    });
  }

  // Getter para calcular el status dinámicamente
  get radioStatus(): 'default' | 'success' | 'error' {
    const control = this.formRadioIndividual.get('planSeleccionado');
    if (control?.invalid && control?.touched) {
      return 'error';
    }
    if (control?.valid && control?.value) {
      return 'success';
    }
    return 'default';
  }

  // Métodos para manejar eventos
  onValueChange(value: any): void {
    console.log('Valor cambiado usando valueChange:', value);
  }

  onChange(event: Event): void {
    console.log('Evento change:', event);
    console.log('Value:', (event.target as HTMLInputElement)?.value);
  }

  onFocus(event: FocusEvent): void {
    console.log('Radio enfocado:', event);
  }

  onBlur(event: FocusEvent): void {
    console.log('Radio desenfocado:', event);
  }

  // Getter para mostrar el texto del plan seleccionado
  get planSeleccionadoTexto(): string {
    const plan = this.formRadioIndividual.get('planSeleccionado')?.value;
    if (plan === 'basic') {
      return 'Plan Básico - $9.99/mes (10GB de almacenamiento)';
    } else if (plan === 'pro') {
      return 'Plan Pro - $29.99/mes (100GB y soporte prioritario)';
    }
    return 'Ningún plan seleccionado';
  }
  
}
```