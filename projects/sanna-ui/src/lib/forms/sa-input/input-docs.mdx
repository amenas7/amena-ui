import { Meta } from '@storybook/blocks';

<Meta title="Componentes/Forms/Input/Input Example" />

# 📚 Code Example - sa-input

Se puede usar los corchetes en las propiedades <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[disabled]`</span>, <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[loading]`</span>, <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[fullWidth]`</span> para controlar el estado del botón desde una variable de tipo boolean. Al igual que para el icono <span style={{ color: '#dc3545', fontWeight: 'bold' }}>`[icon]`</span> y no sólo icon.

### HTML Template
```html
<form [formGroup]="formCompleto">
  <div class="row g-3">
    <div class="col-md-6">
      <sa-input
        label="Input Completo (requerido, mínimo 3 caracteres)"
        formControlName="inputCompleto"
        type="text"
        size="lg"
        [status]="
          formCompleto.get('inputCompleto')?.invalid &&
          formCompleto.get('inputCompleto')?.touched
            ? 'error'
            : formCompleto.get('inputCompleto')?.valid
            ? 'success'
            : 'default'
        "
        [errorText]="
          formCompleto.get('inputCompleto')?.hasError('required')
            ? 'El campo es requerido'
            : formCompleto.get('inputCompleto')?.hasError('minlength')
            ? 'Mínimo 3 caracteres'
            : formCompleto.get('inputCompleto')?.hasError('maxlength')
            ? 'Máximo 50 caracteres'
            : ''
        "
        placeholder="Ingrese un texto..."
        helperText="Este input tiene todas las propiedades configuradas con status dinámico"
        id="input-completo"
        name="inputCompleto"
        autocomplete="off"
        [required]="isRequired"
        [disabled]="isDisabled"
        (valueChange)="onValueChange($event)"
        (change)="onChange($event)"
        (enter)="onEnter($event)"
        (focusin)="onFocus($event)"
        (focusout)="onBlur($event)">
      </sa-input>
    </div>
    <div class="col-md-6">
      <div class="mt-4">
        <p><strong>Estado del campo:</strong></p>
        <ul class="small">
          <li>Valor: {{ formCompleto.get('inputCompleto')?.value || 'vacío' }}</li>
          <li>Valid: {{ formCompleto.get('inputCompleto')?.valid }}</li>
          <li>Touched: {{ formCompleto.get('inputCompleto')?.touched }}</li>
          <li>Status mostrado:
            <sa-tag
              [text]="
                formCompleto.get('inputCompleto')?.invalid && formCompleto.get('inputCompleto')?.touched
                  ? 'error'
                  : formCompleto.get('inputCompleto')?.valid
                  ? 'success'
                  : 'default'
              "
              [type]="
                formCompleto.get('inputCompleto')?.invalid && formCompleto.get('inputCompleto')?.touched
                  ? 'danger'
                  : formCompleto.get('inputCompleto')?.valid
                  ? 'success'
                  : 'secondary'
              "
              size="small">
            </sa-tag>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <div class="row g-3 mt-2">
    <div class="col-md-12">
      <p><strong>Valor actual:</strong> {{ valorInputCompleto }}</p>
      <p><strong>Estado del formulario (mínimo 3 digitos):</strong> {{ formCompleto.valid ? 'Válido' : 'Inválido' }}</p>
    </div>
  </div>

  <div class="row g-3 mt-2">
    <div class="col-md-12">
      <button class="btn btn-sm btn-outline-primary me-2" (click)="isRequired = !isRequired">
        Toggle Required ({{ isRequired }})
      </button>
      <button class="btn btn-sm btn-outline-info me-2" (click)="isDisabled = !isDisabled">
        Toggle Disabled ({{ isDisabled }})
      </button>
    </div>
  </div>
</form>
```

### ⚡ TypeScript Component

```typescript
import { Component, OnInit } from '@angular/core';
import { FormBuilder, FormGroup, Validators } from '@angular/forms';

@Component({
  ......
})

export class ........ {

  // Formulario reactivo para el ejemplo
  formCompleto!: FormGroup;

  // Variables para controles
  isDisabled: boolean = false;
  isRequired: boolean = false;

  constructor(private fb: FormBuilder) {}

  ngOnInit(): void {
    // Inicializar formulario
    this.formCompleto = this.fb.group({
      inputCompleto: ['', [Validators.minLength(3), Validators.maxLength(50)]]
    });
  }

  // Métodos para manejar eventos
  onValueChange(value: string): void {
    console.log('Valor cambiado usando valueChange:', value);
  }

  onChange(event: Event): void {
    console.log('Evento change:', event);
    console.log('Valor del evento usando change:', (event.target as HTMLInputElement)?.value);
  }

  onEnter(event: KeyboardEvent): void {
    const valor = this.formCompleto.get('inputCompleto')?.value;
    console.log('Enter presionado:', event);
    alert('Enter presionado Valor: ' + valor);
  }

  onFocus(event: FocusEvent): void {
    console.log('Input enfocado (onFocus):', event);
  }

  onBlur(event: FocusEvent): void {
    console.log('Input desenfocado (onBlur):', event);
  }

  // Getters para acceder fácilmente a los valores
  get valorInputCompleto(): string {
    return this.formCompleto.get('inputCompleto')?.value || '';
  }
  
}
```